<?php
//login and password for connecting to mysql database
$user = $user ? $user : "h2opouwf"; //if has no value set user to:
$passwd = $passwd ? $passwd : "Random1029"; // set password to:
$db = $db ? $db : "h2opouwf_aquaponics"; // set database to: